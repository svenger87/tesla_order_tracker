openapi: 3.0.3
info:
  title: tff order stats API
  description: |
    External REST API for tff order stats.

    This API allows external systems to create, read, and update Tesla orders.
    All endpoints require API key authentication via the `X-API-Key` header.

    ## Authentication
    Include your API key in the `X-API-Key` header with every request:
    ```
    X-API-Key: your-api-key-here
    ```

    ## Getting access
    API keys are issued on request by the site admin. Each key carries scopes:

    | Scope | Allows |
    |---|---|
    | `orders:read` | List and fetch orders. Names, VINs, TOST ids and delivery locations are omitted. |
    | `orders:read:pii` | Adds those fields, and lookup by name. Requires `orders:read`. |
    | `orders:write` | Create and edit orders. |
    | `options:read` | Read dropdown options. |

    A key without the scope an operation needs gets `403 INSUFFICIENT_SCOPE`.

    Each key has a per-minute limit and a daily quota (UTC day). Responses carry
    `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-Quota-Remaining`; when a
    limit is hit the API answers `429 RATE_LIMITED` with `Retry-After` in seconds.

    ## Response Format
    All responses follow a consistent envelope format:
    ```json
    {
      "success": true,
      "data": { ... },
      "meta": {
        "timestamp": "2025-02-12T10:30:00Z",
        "version": "v1"
      }
    }
    ```

    ## Date Format
    All dates use German format: `DD.MM.YYYY` (e.g., `15.01.2025`)
  version: 1.0.0
  contact:
    name: tff order stats
servers:
  - url: 'https://tff-order-stats.de/api/v1'
    description: Production
  - url: 'http://localhost:3000/api/v1'
    description: Local Development

security:
  - ApiKeyAuth: []

tags:
  - name: Orders
    description: Order management operations
  - name: TOST
    description: |
      TOST (tesla-order-status-tracker.de) integration endpoints.
      These endpoints allow TOST to create, claim, and update orders as a trusted service partner.
      All TOST endpoints require authentication via a separate `TOST_API_KEY`.
  - name: Options
    description: Form dropdown options

paths:
  /orders:
    get:
      tags:
        - Orders
      summary: List all orders
      description: Returns a paginated list of orders with optional filtering. Requires scope `orders:read`.
      operationId: listOrders
      x-required-scope: orders:read
      parameters:
        - name: limit
          in: query
          description: Maximum number of orders to return (1-100)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: offset
          in: query
          description: Number of orders to skip
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: country
          in: query
          description: Filter by country code (e.g., de, at, ch)
          schema:
            type: string
        - name: model
          in: query
          description: Filter by model (standard, premium, performance)
          schema:
            type: string
            enum: [standard, premium, performance]
        - name: archived
          in: query
          description: Include archived orders
          schema:
            type: boolean
            default: false
        - name: vehicleType
          in: query
          description: Filter by vehicle type
          schema:
            type: string
            enum: [Model Y, Model 3]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Order'
                      meta:
                        allOf:
                          - $ref: '#/components/schemas/Meta'
                          - type: object
                            properties:
                              pagination:
                                $ref: '#/components/schemas/Pagination'
              example:
                success: true
                data:
                  - id: "clx123abc"
                    name: "MaxMustermann"
                    vehicleType: "Model Y"
                    orderDate: "15.01.2025"
                    country: "de"
                    model: "performance"
                    range: null
                    drive: "awd"
                    color: "ultra_red"
                    interior: "black"
                    wheels: "21"
                    towHitch: "ja"
                    autopilot: "fsd"
                    seats: "5"
                    source: null
                    tostUserId: null
                    deliveryWindow: "Februar 2025"
                    deliveryLocation: "München"
                    vin: null
                    vinReceivedDate: null
                    papersReceivedDate: null
                    productionDate: null
                    typeApproval: null
                    typeVariant: null
                    deliveryDate: null
                    orderToProduction: null
                    orderToVin: null
                    orderToDelivery: null
                    orderToPapers: null
                    papersToDelivery: null
                    archived: false
                    archivedAt: null
                    createdAt: "2025-02-12T10:30:00.000Z"
                    updatedAt: "2025-02-12T10:30:00.000Z"
                meta:
                  timestamp: "2025-02-12T10:30:00.000Z"
                  version: "v1"
                  pagination:
                    total: 150
                    limit: 50
                    offset: 0
                    hasMore: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

    post:
      tags:
        - Orders
      summary: Create a new order
      description: Creates a new order with the provided details. Requires scope `orders:write`.
      operationId: createOrder
      x-required-scope: orders:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrderRequest'
            example:
              name: "MaxMustermann"
              vehicleType: "Model Y"
              orderDate: "15.01.2025"
              country: "de"
              model: "performance"
              drive: "awd"
              color: "ultra_red"
              interior: "black"
              wheels: "21"
              towHitch: "ja"
              autopilot: "fsd"
              seats: "5"
              deliveryWindow: "Februar 2025"
              deliveryLocation: "München"
              editCode: "MySecure123"
      responses:
        '201':
          description: Order created successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CreateOrderResponse'
              example:
                success: true
                data:
                  id: "clx789ghi"
                  editCode: "MySecure123"
                  message: "Order created successfully"
                meta:
                  timestamp: "2025-02-12T10:30:00.000Z"
                  version: "v1"
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

  /orders/{id}:
    get:
      tags:
        - Orders
      summary: Get order by ID
      description: Returns a single order by its ID. Requires scope `orders:read`.
      operationId: getOrderById
      x-required-scope: orders:read
      parameters:
        - name: id
          in: path
          required: true
          description: Order ID
          schema:
            type: string
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Order'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

    put:
      tags:
        - Orders
      summary: Update an order
      description: Updates an existing order. Requires the order's edit code for authorization. Requires scope `orders:write`.
      operationId: updateOrder
      x-required-scope: orders:write
      parameters:
        - name: id
          in: path
          required: true
          description: Order ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrderRequest'
            example:
              editCode: "MySecure123"
              vin: "5YJ3E1EA1NF123456"
              vinReceivedDate: "10.02.2025"
              deliveryWindow: "Februar 2025"
      responses:
        '200':
          description: Order updated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/UpdateOrderResponse'
              example:
                success: true
                data:
                  id: "clx123abc"
                  updatedAt: "2025-02-12T10:35:00.000Z"
                  message: "Order updated successfully"
                meta:
                  timestamp: "2025-02-12T10:35:00.000Z"
                  version: "v1"
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

  /orders/by-name/{name}:
    get:
      tags:
        - Orders
      summary: Get orders by username
      description: |
        Returns all orders for a given username. Returns an array since
        a user can have multiple orders with different order dates.
        Username matching is case-insensitive.
      operationId: getOrdersByName
      x-required-scope: orders:read:pii
      parameters:
        - name: name
          in: path
          required: true
          description: Username (URL-encoded)
          schema:
            type: string
        - name: archived
          in: query
          description: Include archived orders
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Order'
                      meta:
                        allOf:
                          - $ref: '#/components/schemas/Meta'
                          - type: object
                            properties:
                              count:
                                type: integer
                                description: Number of orders found
              example:
                success: true
                data:
                  - id: "clx123abc"
                    name: "MaxMustermann"
                    orderDate: "15.01.2025"
                    country: "de"
                    model: "performance"
                  - id: "clx456def"
                    name: "MaxMustermann"
                    orderDate: "20.06.2024"
                    country: "de"
                    model: "premium"
                meta:
                  timestamp: "2025-02-12T10:30:00.000Z"
                  version: "v1"
                  count: 2
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

  /tost/orders:
    get:
      tags:
        - TOST
      summary: Find orders by ID, username, or TOST user ID
      description: |
        Returns orders matching the given filter(s). At least one filter is required.
        Supports three filter modes:
        - `id` — look up a specific order by its ID (e.g., TOST-generated hash of RN)
        - `name` — find all orders for a username
        - `tostUserId` — find all orders for a TOST user (Tesla account)

        Filters can be combined. Returns an empty array if no orders match.
      operationId: tostFindOrders
      x-required-scope: tost
      security:
        - TostApiKeyAuth: []
      parameters:
        - name: id
          in: query
          description: Order ID (exact match, e.g., TOST-generated hash)
          schema:
            type: string
        - name: name
          in: query
          description: Username to search for (exact match)
          schema:
            type: string
        - name: tostUserId
          in: query
          description: TOST user ID to search for (all orders for this Tesla account)
          schema:
            type: string
      responses:
        '200':
          description: Orders found (may be empty array)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Order'
                      meta:
                        allOf:
                          - $ref: '#/components/schemas/Meta'
                          - type: object
                            properties:
                              count:
                                type: integer
                                description: Number of orders found
              example:
                success: true
                data:
                  - id: "tost_rn_hash_abc123"
                    name: "MaxMustermann"
                    vehicleType: "Model Y"
                    orderDate: "15.01.2025"
                    source: "tost"
                    tostUserId: "tost_user_abc123"
                meta:
                  timestamp: "2026-03-13T10:30:00.000Z"
                  version: "v1"
                  count: 1
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '500':
          $ref: '#/components/responses/ServerError'

    post:
      tags:
        - TOST
      summary: Create or auto-claim a TOST-managed order
      description: |
        Creates a new order or auto-claims an existing one.

        **Auto-claim:** If an order with the same `name` already exists (not archived),
        it will be automatically claimed by TOST (`source` set to `"tost"`) and all fields
        will be overwritten with the provided data — including `orderDate`.
        The response includes `claimed: true` so TOST knows an existing order was taken over.

        **New order:** If no matching order exists, a new one is created with `source` set to `"tost"`.
        The response includes `claimed: false`.

        **Custom ID:** You can provide your own `id` (e.g., a hash of RN + cipher) instead
        of letting the system generate one. Only used for new orders (not auto-claims, which keep the existing ID).
        If an order with the given ID already exists, returns 409 Conflict.

        No editCode is required. The order will be exclusively managed by TOST.
      operationId: tostCreateOrder
      x-required-scope: tost
      security:
        - TostApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TostCreateOrderRequest'
            example:
              id: "tost_rn_hash_abc123"
              name: "MaxMustermann"
              vehicleType: "Model Y"
              orderDate: "15.01.2025"
              country: "de"
              model: "performance"
              drive: "awd"
              color: "ultra_red"
              interior: "black"
              wheels: "21"
              towHitch: "ja"
              autopilot: "fsd"
              seats: "5"
              tostUserId: "tost_user_abc123"
      responses:
        '200':
          description: Existing order auto-claimed and updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          id:
                            type: string
                            description: Existing order ID (unchanged)
                          message:
                            type: string
                          claimed:
                            type: boolean
                            description: true when an existing order was auto-claimed
              example:
                success: true
                data:
                  id: "cmlgpaj2c002s5oxugajnuxu3"
                  message: "Existing order claimed and updated by TOST"
                  claimed: true
                meta:
                  timestamp: "2026-03-14T10:30:00.000Z"
                  version: "v1"
        '201':
          description: New order created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          id:
                            type: string
                            description: Order ID (your provided ID or auto-generated)
                          message:
                            type: string
                          claimed:
                            type: boolean
                            description: false when a new order was created
              example:
                success: true
                data:
                  id: "tost_rn_hash_abc123"
                  message: "Order created successfully"
                  claimed: false
                meta:
                  timestamp: "2026-03-14T10:30:00.000Z"
                  version: "v1"
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '500':
          $ref: '#/components/responses/ServerError'

  /tost/orders/{id}:
    put:
      tags:
        - TOST
      summary: Update a TOST-owned order
      description: |
        Updates an order that is owned by TOST (`source = "tost"`).
        Returns 403 if the order is not TOST-managed — claim it first via `POST /tost/claim/{id}`.
        No editCode is required. Supports optimistic locking via `expectedUpdatedAt`.
      operationId: tostUpdateOrder
      x-required-scope: tost
      security:
        - TostApiKeyAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Order ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TostUpdateOrderRequest'
            example:
              vin: "5YJ3E1EA1NF123456"
              vinReceivedDate: "10.02.2025"
              deliveryWindow: "März 2025"
      responses:
        '200':
          description: Order updated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/UpdateOrderResponse'
              example:
                success: true
                data:
                  id: "clx123abc"
                  updatedAt: "2026-03-13T10:35:00.000Z"
                  message: "Order updated successfully"
                meta:
                  timestamp: "2026-03-13T10:35:00.000Z"
                  version: "v1"
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/ServerError'

    delete:
      tags:
        - TOST
      summary: Delete a TOST-owned order
      description: |
        Permanently deletes an order that is owned by TOST (`source = "tost"`).
        Returns 403 if the order is not TOST-managed.
      operationId: tostDeleteOrder
      x-required-scope: tost
      security:
        - TostApiKeyAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Order ID to delete
          schema:
            type: string
      responses:
        '200':
          description: Order deleted successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          id:
                            type: string
                          message:
                            type: string
              example:
                success: true
                data:
                  id: "tost_rn_hash_abc123"
                  message: "Order deleted successfully"
                meta:
                  timestamp: "2026-03-14T10:30:00.000Z"
                  version: "v1"
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/ServerError'

  /tost/claim/{id}:
    post:
      tags:
        - TOST
      summary: Claim an existing order for TOST
      description: |
        Claims an existing order so it becomes TOST-managed.
        Sets `source` to `"tost"` and optionally associates a `tostUserId`.
        Returns 409 if the order is already claimed by TOST.
        After claiming, the order can no longer be edited via the webapp.
      operationId: tostClaimOrder
      x-required-scope: tost
      security:
        - TostApiKeyAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Order ID to claim
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tostUserId:
                  type: string
                  description: TOST's internal user identifier (optional)
                newId:
                  type: string
                  description: |
                    Replace the order's ID with a TOST-generated one (e.g., hash of RN + cipher).
                    This re-keys the order so TOST can use deterministic IDs for claimed orders too.
                    If omitted, the order keeps its original webapp-generated ID.
            example:
              tostUserId: "tost_user_abc123"
              newId: "tost_rn_hash_def456"
      responses:
        '200':
          description: Order claimed successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          id:
                            type: string
                          name:
                            type: string
                          updatedAt:
                            type: string
                            format: date-time
                          message:
                            type: string
              example:
                success: true
                data:
                  id: "clx123abc"
                  name: "MaxMustermann"
                  updatedAt: "2026-03-13T10:30:00.000Z"
                  message: "Order claimed successfully"
                meta:
                  timestamp: "2026-03-13T10:30:00.000Z"
                  version: "v1"
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '500':
          $ref: '#/components/responses/ServerError'

  /options:
    get:
      tags:
        - Options
      summary: Get dropdown options
      description: Returns all available dropdown options for the order form, grouped by type. Requires scope `options:read`.
      operationId: getOptions
      x-required-scope: options:read
      parameters:
        - name: type
          in: query
          description: Filter by option type
          schema:
            type: string
            enum:
              - country
              - model
              - range
              - drive
              - color
              - interior
              - wheels
              - autopilot
              - towHitch
              - seats
              - deliveryLocation
        - name: vehicleType
          in: query
          description: Filter options by vehicle type (returns vehicle-specific and universal options)
          schema:
            type: string
            enum: [Model Y, Model 3]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Options'
              example:
                success: true
                data:
                  country:
                    - value: "de"
                      label: "Deutschland"
                      vehicleType: null
                      metadata:
                        flag: "de"
                    - value: "at"
                      label: "Österreich"
                      vehicleType: null
                      metadata:
                        flag: "at"
                  model:
                    - value: "standard"
                      label: "Standard"
                      vehicleType: "Model Y"
                    - value: "premium"
                      label: "Premium"
                      vehicleType: "Model Y"
                    - value: "performance"
                      label: "Performance"
                      vehicleType: "Model Y"
                    - value: "model_3"
                      label: "Model 3"
                      vehicleType: "Model 3"
                    - value: "model_3_lr"
                      label: "Model 3 Long Range"
                      vehicleType: "Model 3"
                    - value: "model_3_performance"
                      label: "Model 3 Performance"
                      vehicleType: "Model 3"
                  color:
                    - value: "pearl_white"
                      label: "Pearl White"
                      metadata:
                        hex: "#FFFFFF"
                        border: true
                    - value: "ultra_red"
                      label: "Ultra Red"
                      metadata:
                        hex: "#C41E3A"
                        border: false
                meta:
                  timestamp: "2025-02-12T10:30:00.000Z"
                  version: "v1"
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/InsufficientScope'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: |
        Per-consumer API key, sent as `X-API-Key`. Keys are issued on request
        by the site admin; each key carries scopes and rate limits (see
        "Getting access" in the API description).
    TostApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: Dedicated API key for TOST integration (separate from general API key)

  schemas:
    Order:
      type: object
      properties:
        id:
          type: string
          description: Unique order identifier
        name:
          type: string
          description: Customer username. Only present for keys with orders:read:pii.
        vehicleType:
          type: string
          nullable: true
          enum: [Model Y, Model 3]
          description: Vehicle type (Model Y or Model 3)
        orderDate:
          type: string
          nullable: true
          description: Order date (DD.MM.YYYY format)
        country:
          type: string
          nullable: true
          description: Country code
        model:
          type: string
          nullable: true
          description: |
            Tesla model trim. Values depend on vehicleType:
            - Model Y: standard, premium, performance
            - Model 3: model_3, model_3_lr, model_3_performance
        range:
          type: string
          nullable: true
          enum: [maximale_reichweite, standard]
          description: Range option (not applicable for Performance)
        drive:
          type: string
          nullable: true
          enum: [rwd, awd]
          description: Drive type
        color:
          type: string
          nullable: true
          description: Exterior color
        interior:
          type: string
          nullable: true
          enum: [black, white]
          description: Interior color
        wheels:
          type: string
          nullable: true
          enum: ['18', '19', '20', '21']
          description: Wheel size
        towHitch:
          type: string
          nullable: true
          enum: [ja, nein, nv]
          description: Tow hitch option
        autopilot:
          type: string
          nullable: true
          enum: [none, ap, eap, eap_transfer, fsd, fsd_transfer]
          description: Autopilot package
        seats:
          type: string
          nullable: true
          enum: ['5', '7']
          description: Seat configuration (5 or 7 seater, 7 only for Model Y Premium AWD)
        source:
          type: string
          nullable: true
          enum: [tost]
          description: Order source — "tost" for TOST-managed orders, null for manually created orders
        tostUserId:
          type: string
          nullable: true
          description: TOST's internal user identifier (only set for TOST-managed orders). Only present for keys with orders:read:pii.
        deliveryWindow:
          type: string
          nullable: true
          description: Expected delivery window
        deliveryLocation:
          type: string
          nullable: true
          description: Delivery location. Only present for keys with orders:read:pii.
        vin:
          type: string
          nullable: true
          description: Vehicle Identification Number. Only present for keys with orders:read:pii.
        vinReceivedDate:
          type: string
          nullable: true
          description: Date VIN was received (DD.MM.YYYY)
        papersReceivedDate:
          type: string
          nullable: true
          description: Date papers were received (DD.MM.YYYY)
        productionDate:
          type: string
          nullable: true
          description: Production date (DD.MM.YYYY)
        typeApproval:
          type: string
          nullable: true
          description: Type approval code
        typeVariant:
          type: string
          nullable: true
          description: Type variant code
        deliveryDate:
          type: string
          nullable: true
          description: Actual delivery date (DD.MM.YYYY)
        orderToProduction:
          type: integer
          nullable: true
          description: Days from order to production
        orderToVin:
          type: integer
          nullable: true
          description: Days from order to VIN received
        orderToDelivery:
          type: integer
          nullable: true
          description: Days from order to delivery
        orderToPapers:
          type: integer
          nullable: true
          description: Days from order to papers received
        papersToDelivery:
          type: integer
          nullable: true
          description: Days from papers to delivery
        archived:
          type: boolean
          description: Whether the order is archived
        archivedAt:
          type: string
          format: date-time
          nullable: true
          description: Archive timestamp
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp

    CreateOrderRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: Customer username (required)
        vehicleType:
          type: string
          enum: [Model Y, Model 3]
          description: Vehicle type (defaults to Model Y)
        orderDate:
          type: string
          description: Order date (DD.MM.YYYY format)
        country:
          type: string
        model:
          type: string
          description: |
            Model trim. Values depend on vehicleType:
            - Model Y: standard, premium, performance
            - Model 3: model_3, model_3_lr, model_3_performance
        range:
          type: string
          enum: [maximale_reichweite, standard]
        drive:
          type: string
          enum: [rwd, awd]
        color:
          type: string
        interior:
          type: string
          enum: [black, white]
        wheels:
          type: string
          enum: ['18', '19', '20', '21']
        towHitch:
          type: string
          enum: [ja, nein, nv]
        autopilot:
          type: string
          enum: [none, ap, eap, eap_transfer, fsd, fsd_transfer]
        seats:
          type: string
          enum: ['5', '7']
          description: Seat configuration (5 or 7 seater)
        deliveryWindow:
          type: string
        deliveryLocation:
          type: string
        vin:
          type: string
        vinReceivedDate:
          type: string
        papersReceivedDate:
          type: string
        productionDate:
          type: string
        typeApproval:
          type: string
        typeVariant:
          type: string
        deliveryDate:
          type: string
        editCode:
          type: string
          description: Custom edit password (min 6 chars, must contain a number)

    UpdateOrderRequest:
      type: object
      required:
        - editCode
      properties:
        editCode:
          type: string
          description: Edit code for authorization (required)
        name:
          type: string
        vehicleType:
          type: string
          enum: [Model Y, Model 3]
          description: Vehicle type
        orderDate:
          type: string
        country:
          type: string
        model:
          type: string
        range:
          type: string
        drive:
          type: string
        color:
          type: string
        interior:
          type: string
        wheels:
          type: string
        towHitch:
          type: string
        autopilot:
          type: string
        seats:
          type: string
          enum: ['5', '7']
        deliveryWindow:
          type: string
        deliveryLocation:
          type: string
        vin:
          type: string
        vinReceivedDate:
          type: string
        papersReceivedDate:
          type: string
        productionDate:
          type: string
        typeApproval:
          type: string
        typeVariant:
          type: string
        deliveryDate:
          type: string
        expectedUpdatedAt:
          type: string
          format: date-time
          description: For optimistic locking - fails if order was modified after this time

    CreateOrderResponse:
      type: object
      properties:
        id:
          type: string
          description: Created order ID
        editCode:
          type: string
          nullable: true
          description: Edit code for future updates
        message:
          type: string

    UpdateOrderResponse:
      type: object
      properties:
        id:
          type: string
          description: Updated order ID
        updatedAt:
          type: string
          format: date-time
          description: New update timestamp
        message:
          type: string

    TostCreateOrderRequest:
      type: object
      required:
        - name
      properties:
        id:
          type: string
          description: Custom order ID (e.g., hash of RN + cipher). If omitted, auto-generated. Must be unique.
        name:
          type: string
          description: Customer username (required)
        vehicleType:
          type: string
          enum: [Model Y, Model 3]
          description: Vehicle type (defaults to Model Y)
        orderDate:
          type: string
          description: Order date (DD.MM.YYYY format)
        country:
          type: string
        model:
          type: string
        range:
          type: string
          enum: [maximale_reichweite, standard]
        drive:
          type: string
          enum: [rwd, awd]
        color:
          type: string
        interior:
          type: string
          enum: [black, white]
        wheels:
          type: string
          enum: ['18', '19', '20', '21']
        towHitch:
          type: string
          enum: [ja, nein, nv]
        autopilot:
          type: string
          enum: [none, ap, eap, eap_transfer, fsd, fsd_transfer]
        seats:
          type: string
          enum: ['5', '7']
        deliveryWindow:
          type: string
        deliveryLocation:
          type: string
        vin:
          type: string
        vinReceivedDate:
          type: string
        productionDate:
          type: string
        deliveryDate:
          type: string
        tostUserId:
          type: string
          description: TOST's internal user identifier (optional)

    TostUpdateOrderRequest:
      type: object
      description: |
        Note: `papersReceivedDate`, `typeApproval`, and `typeVariant` are NOT accepted
        by this endpoint — TOST cannot provide these fields. Users edit them via the webapp.
      properties:
        name:
          type: string
        vehicleType:
          type: string
          enum: [Model Y, Model 3]
        orderDate:
          type: string
        country:
          type: string
        model:
          type: string
        range:
          type: string
        drive:
          type: string
        color:
          type: string
        interior:
          type: string
        wheels:
          type: string
        towHitch:
          type: string
        autopilot:
          type: string
        seats:
          type: string
        deliveryWindow:
          type: string
        deliveryLocation:
          type: string
        vin:
          type: string
        vinReceivedDate:
          type: string
        productionDate:
          type: string
        deliveryDate:
          type: string
        tostUserId:
          type: string
          description: TOST's internal user identifier
        expectedUpdatedAt:
          type: string
          format: date-time
          description: For optimistic locking — fails with 409 if order was modified after this time

    Option:
      type: object
      properties:
        value:
          type: string
          description: Option value
        label:
          type: string
          description: Display label
        vehicleType:
          type: string
          nullable: true
          enum: [Model Y, Model 3]
          description: Which vehicle this option applies to (null = all vehicles)
        metadata:
          type: object
          description: Additional metadata (e.g., hex color, flag emoji)

    Options:
      type: object
      properties:
        country:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        model:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        range:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        drive:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        color:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        interior:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        wheels:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        autopilot:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        towHitch:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        seats:
          type: array
          items:
            $ref: '#/components/schemas/Option'
        deliveryLocation:
          type: array
          items:
            $ref: '#/components/schemas/Option'

    Meta:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        version:
          type: string
          example: "v1"

    Pagination:
      type: object
      properties:
        total:
          type: integer
          description: Total number of items
        limit:
          type: integer
          description: Items per page
        offset:
          type: integer
          description: Items skipped
        hasMore:
          type: boolean
          description: Whether more items exist

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object
        meta:
          $ref: '#/components/schemas/Meta'

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error:
          type: object
          properties:
            code:
              type: string
              description: Error code
            message:
              type: string
              description: Human-readable error message
            details:
              type: object
              description: Field-specific error details
        meta:
          $ref: '#/components/schemas/Meta'

  responses:
    InsufficientScope:
      description: The key is valid but lacks the scope this operation requires.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "INSUFFICIENT_SCOPE"
              message: 'This API key lacks the "orders:write" scope'
            meta:
              timestamp: "2026-09-19T12:00:00.000Z"
              version: "v1"

    RateLimited:
      description: Per-minute limit or daily quota exceeded.
      headers:
        Retry-After:
          description: Seconds until the request may be retried.
          schema:
            type: integer
        X-RateLimit-Limit:
          description: Requests allowed per minute for this key.
          schema:
            type: integer
        X-RateLimit-Remaining:
          description: Requests left in the current minute.
          schema:
            type: integer
        X-Quota-Remaining:
          description: Requests left today (UTC).
          schema:
            type: integer
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "RATE_LIMITED"
              message: "Rate limit of 60 requests per minute exceeded"
            meta:
              timestamp: "2026-09-19T12:00:00.000Z"
              version: "v1"

    Unauthorized:
      description: API key missing or invalid
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "UNAUTHORIZED"
              message: "API key required. Use X-API-Key header."
            meta:
              timestamp: "2025-02-12T10:30:00.000Z"
              version: "v1"

    Forbidden:
      description: Action not allowed (e.g., updating a non-TOST order via TOST API)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "FORBIDDEN"
              message: "Order is not managed by TOST. Claim it first."
            meta:
              timestamp: "2026-03-13T10:30:00.000Z"
              version: "v1"

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "NOT_FOUND"
              message: "Order not found"
            meta:
              timestamp: "2025-02-12T10:30:00.000Z"
              version: "v1"

    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "VALIDATION_ERROR"
              message: "Validation failed"
              details:
                name: "Name is required"
            meta:
              timestamp: "2025-02-12T10:30:00.000Z"
              version: "v1"

    Conflict:
      description: Conflict (e.g., concurrent modification)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "CONFLICT"
              message: "Order was modified by another user. Please refresh and try again."
            meta:
              timestamp: "2025-02-12T10:30:00.000Z"
              version: "v1"

    ServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: "SERVER_ERROR"
              message: "Internal server error"
            meta:
              timestamp: "2025-02-12T10:30:00.000Z"
              version: "v1"
